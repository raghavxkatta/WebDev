let list=prompt("What would you like to do?");
while(prompt(!list)){
    if(prompt.toLowerCase==="quit")break;
    else if(prompt(list)==='list'){
console.log(list);
    }
    else{
console.log=(`${list} added to the list`);
    }

}
